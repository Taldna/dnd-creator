"use client"

import type { Ability } from "../../types/data/ability"

export default function AbilitiesSelection({
  setAbilities,
}: {
  setAbilities: (abilities: Ability) => void
}) {
  return <div>Abilities Selection for future implementation</div>
}
